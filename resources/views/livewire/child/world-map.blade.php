<div class="min-h-screen bg-gradient-to-b from-sky-200 to-green-200 p-5"><div class="max-w-3xl mx-auto"><header class="flex justify-between items-center"><a href="{{ route('child.home') }}" class="bg-white rounded-full w-12 h-12 grid place-items-center shadow">←</a><h1 class="text-2xl font-black">Adventure Map</h1><div class="bg-white px-4 py-2 rounded-full">⭐ {{ $child->stars }}</div></header>@foreach($worlds as $world)<section class="py-12 text-center"><div class="text-6xl">{{ $world->icon }}</div><h2 class="text-3xl font-black">{{ $world->name }}</h2><p class="text-slate-600">{{ $world->description }}</p><div class="mt-8 flex flex-col items-center gap-5">@foreach($world->levels as $level)@php($p=$level->progress->first())@php($unlocked=$level->level_number <= $child->current_level)<a @if($unlocked) href="{{ route('game.level',$level) }}" @endif class="{{ $unlocked?'bg-white':'bg-slate-200 opacity-60' }} w-64 rounded-3xl p-5 shadow-lg"><div class="text-5xl">{{ $unlocked?$level->icon:'🔒' }}</div><div class="font-black text-xl mt-2">{{ $level->name }}</div><div class="text-sm text-slate-500">Level {{ $level->level_number }}</div>@if($p)<div class="mt-2">@for($i=1;$i<=3;$i++){{ $i <= $p->stars?'⭐':'☆' }}@endfor</div>@endif</a>@endforeach</div></section>@endforeach</div></div>
