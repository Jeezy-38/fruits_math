<div class="relative overflow-hidden min-h-screen p-5"><x-animated-landscape world="fruit-garden" /><div class="relative z-10 max-w-3xl mx-auto"><header class="flex justify-between items-center"><a href="{{ route('child.home') }}" class="bg-white/90 backdrop-blur rounded-full w-12 h-12 grid place-items-center shadow font-bold hover:scale-105 transition">←</a><h1 class="text-2xl font-black bg-white/80 backdrop-blur px-6 py-2 rounded-full shadow-sm text-slate-800">🗺️ Adventure Map</h1><div class="flex items-center gap-2"><a href="{{ route('language', app()->getLocale() === 'sw' ? 'en' : 'sw') }}" class="bg-white/90 hover:bg-white backdrop-blur px-3.5 py-2 rounded-full shadow text-xs font-black text-slate-700 hover:scale-105 active:scale-95 transition flex items-center gap-1.5 border border-white/60"><span>🌐</span><span>{{ app()->getLocale() === 'sw' ? 'English' : 'Kiswahili' }}</span></a><div class="bg-white/90 backdrop-blur px-5 py-2 rounded-full font-black text-amber-500 shadow">⭐ {{ $child->stars }}</div></div></header>@foreach($worlds as $world)<section class="py-12 text-center"><div class="text-6xl drop-shadow-md">{{ $world->icon }}</div><h2 class="text-3xl font-black text-slate-900 mt-2">{{ $world->name }}</h2><p class="text-slate-600 font-semibold mt-1">{{ $world->description }}</p><div class="mt-8 flex flex-col items-center gap-5">@foreach($world->levels as $level)@php($p=$level->progress->first())@php($unlocked=$level->level_number <= $child->current_level)<a @if($unlocked) href="{{ route('game.level',$level) }}" @endif class="{{ $unlocked?'bg-white/95 backdrop-blur shadow-xl hover:scale-105 border-2 border-white':'bg-slate-200/80 backdrop-blur opacity-60' }} w-64 rounded-3xl p-5 transition-all"><div class="text-5xl">{{ $unlocked?$level->icon:'🔒' }}</div><div class="font-black text-xl mt-2 text-slate-800">{{ $level->name }}</div><div class="text-sm text-slate-500 font-bold">Level {{ $level->level_number }}</div>@if($p)<div class="mt-2 text-amber-400 text-lg">@for($i=1;$i<=3;$i++){{ $i <= $p->stars?'⭐':'☆' }}@endfor</div>@endif</a>@endforeach</div></section>@endforeach</div></div>
