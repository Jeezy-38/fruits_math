<div class="max-w-6xl mx-auto p-6"><h1 class="text-3xl font-black">Fruit Math Admin</h1><p class="text-slate-500 mb-8">Platform overview and curriculum status.</p><a href="{{ route('admin.curriculum') }}" class="inline-block mb-6 font-bold text-emerald-700">Manage curriculum →</a><div class="grid grid-cols-2 md:grid-cols-3 gap-4">@foreach($stats as $label=>$value)<div class="bg-white rounded-3xl p-6 shadow"><div class="text-3xl font-black">{{ number_format($value) }}</div><div class="capitalize text-slate-500">{{ $label }}</div></div>@endforeach</div></div>